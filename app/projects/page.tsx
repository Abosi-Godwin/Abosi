import Projects from "../components/Projects";

interface Props {}

const Projects = (props: Props) => {
    return (
        <div>
            <Projects />
        </div>
    );
};

export default Projects;
